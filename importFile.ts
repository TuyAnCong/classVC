import { File, Write } from 'ServerFile'
const App = require('ServerFile')
var ServerFile.Folder = '/home'
File('IMPORT', import_file(JSON.phrse('http://cdn.discordcard.com/image/welcome?user=' + userid), 'image.png', App.ServerUptime.SaveFile(uptime)))
App.Binary.Server(folder, pingus).join('.')
App.Res.Server(${ServerFile.Folder})
